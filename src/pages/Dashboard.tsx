import { ArrowUpRight, CheckCircle2, Clock3, Plus, Sparkles, Target, TrendingUp, Users } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Avatar, Button } from '@/components/ui';
import { useBoardStore } from '@/stores/boardStore';
import { useAuthStore } from '@/stores/authStore';

export function Dashboard(): JSX.Element {
  const { tasks } = useBoardStore(); const { user } = useAuthStore(); const done = tasks.filter((task) => task.status === 'done').length; const active = tasks.filter((task) => task.status === 'in-progress' || task.status === 'review').length; const progress = tasks.length ? Math.round((done / tasks.length) * 100) : 0;
  const upcoming = [...tasks].filter((task) => task.status !== 'done').slice(0, 4);
  return <div className="mx-auto max-w-[1480px] space-y-6 animate-fade-in"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-sm font-semibold text-primary-600">Tuesday, June 18</p><h2 className="font-display text-2xl font-extrabold tracking-tight md:text-3xl">Good morning, {user?.name?.split(' ')[0] ?? 'there'}.</h2><p className="mt-2 text-sm text-slate-500">Here's what's happening with your sprint today.</p></div><Link to="/board"><Button><Plus size={17} />New task</Button></Link></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard icon={<Target size={19} />} label="Sprint progress" value={`${progress}%`} detail="of 10 tasks completed" color="blue" trend="+12.5%" /><StatCard icon={<Clock3 size={19} />} label="In progress" value={String(active)} detail="tasks need attention" color="orange" trend="2 due soon" /><StatCard icon={<CheckCircle2 size={19} />} label="Completed" value={String(done)} detail="tasks shipped this sprint" color="green" trend="On track" /><StatCard icon={<Users size={19} />} label="Team capacity" value="82%" detail="across 4 contributors" color="slate" trend="Healthy" /></div><div className="grid gap-6 xl:grid-cols-[1.6fr_1fr]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 md:p-6"><div className="mb-6 flex items-center justify-between"><div><h3 className="font-display text-base font-bold">Sprint progress</h3><p className="mt-1 text-xs text-slate-400">Sprint 24 · Jun 10 — Jun 21</p></div><Link to="/analytics" className="flex items-center gap-1 text-xs font-bold text-primary-600">View analytics <ArrowUpRight size={14} /></Link></div><div className="flex flex-col items-center gap-7 sm:flex-row sm:gap-12"><div className="relative flex h-44 w-44 shrink-0 items-center justify-center rounded-full" style={{ background: `conic-gradient(#3b6bff ${progress * 3.6}deg, #e9edf5 0deg)` }}><div className="flex h-32 w-32 flex-col items-center justify-center rounded-full bg-white dark:bg-slate-900"><span className="font-display text-3xl font-extrabold">{progress}%</span><span className="text-xs text-slate-400">complete</span></div></div><div className="grid w-full grid-cols-2 gap-4"><ProgressLegend color="bg-primary-500" label="Completed" value={done} /><ProgressLegend color="bg-accent-500" label="In progress" value={tasks.filter((task) => task.status === 'in-progress').length} /><ProgressLegend color="bg-slate-300" label="Review" value={tasks.filter((task) => task.status === 'review').length} /><ProgressLegend color="bg-slate-200" label="Backlog" value={tasks.filter((task) => task.status === 'backlog').length} /></div></div></section><section className="rounded-2xl bg-primary-700 p-6 text-white shadow-lg shadow-primary-200 dark:shadow-none"><div className="mb-6 flex items-start justify-between"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-white/15"><Sparkles size={19} /></div><span className="rounded-full bg-white/15 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider">Focus</span></div><h3 className="font-display text-xl font-bold leading-tight">Keep the momentum going.</h3><p className="mt-3 text-sm leading-relaxed text-primary-100">Your team is ahead of schedule. There are 2 tasks waiting for review.</p><Link to="/board" className="mt-7 inline-flex items-center gap-2 text-sm font-bold text-white hover:text-primary-100">Review tasks <ArrowUpRight size={16} /></Link></section></div><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 md:p-6"><div className="mb-5 flex items-center justify-between"><div><h3 className="font-display text-base font-bold">Upcoming work</h3><p className="mt-1 text-xs text-slate-400">Your next tasks at a glance</p></div><Link to="/board" className="text-xs font-bold text-primary-600">View all</Link></div><div className="divide-y divide-slate-100 dark:divide-slate-800">{upcoming.map((task) => <div key={task.id} className="flex items-center gap-3 py-3"><div className={`h-2 w-2 rounded-full ${task.priority === 'high' ? 'bg-error-500' : task.priority === 'medium' ? 'bg-accent-500' : 'bg-primary-400'}`} /><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{task.title}</p><p className="mt-0.5 text-xs text-slate-400">{task.id} · Due {task.dueDate}</p></div><span className="hidden rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold capitalize text-slate-500 sm:block dark:bg-slate-800">{task.status.replace('-', ' ')}</span><Avatar initials={task.assignee.split(' ').map((name) => name[0]).join('')} size="sm" /></div>)}</div></section></div>;
}
function StatCard({ icon, label, value, detail, color, trend }: { icon: JSX.Element; label: string; value: string; detail: string; color: 'blue' | 'orange' | 'green' | 'slate'; trend: string }): JSX.Element { const colors = { blue: 'bg-primary-50 text-primary-600 dark:bg-primary-950/50', orange: 'bg-accent-50 text-accent-600 dark:bg-accent-950/30', green: 'bg-success-50 text-success-600 dark:bg-success-950/30', slate: 'bg-slate-100 text-slate-600 dark:bg-slate-800' }; return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center justify-between"><div className={`flex h-9 w-9 items-center justify-center rounded-xl ${colors[color]}`}>{icon}</div><span className="flex items-center gap-1 text-[10px] font-bold text-success-600"><TrendingUp size={12} />{trend}</span></div><p className="mt-4 text-xs font-semibold text-slate-400">{label}</p><p className="mt-1 font-display text-2xl font-extrabold">{value}</p><p className="mt-1 text-xs text-slate-400">{detail}</p></div>; }
function ProgressLegend({ color, label, value }: { color: string; label: string; value: number }): JSX.Element { return <div className="flex items-center gap-2"><span className={`h-2 w-2 rounded-full ${color}`} /><span className="text-xs text-slate-500">{label}</span><span className="ml-auto text-sm font-bold">{value}</span></div>; }
