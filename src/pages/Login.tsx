import { FormEvent, useEffect, useState } from 'react';
import { ArrowRight, Eye, EyeOff, LockKeyhole, Sparkles } from 'lucide-react';
import { Navigate, useNavigate } from 'react-router-dom';
import { Button, Input } from '@/components/ui';
import { useAuthStore } from '@/stores/authStore';

export function Login(): JSX.Element {
  const { user, login, isLoading, error } = useAuthStore(); const navigate = useNavigate(); const [username, setUsername] = useState('emilys'); const [password, setPassword] = useState('emilyspass'); const [showPassword, setShowPassword] = useState(false);
  useEffect(() => { if (user) navigate('/dashboard', { replace: true }); }, [user, navigate]);
  if (user) return <Navigate to="/dashboard" replace />;
  const submit = async (event: FormEvent) => { event.preventDefault(); if (await login(username, password)) navigate('/dashboard', { replace: true }); };
  return <div className="grid min-h-screen lg:grid-cols-[1.05fr_0.95fr]"><div className="relative hidden overflow-hidden bg-primary-950 p-12 text-white lg:flex lg:flex-col"><div className="absolute -right-28 -top-28 h-96 w-96 rounded-full bg-primary-500/20 blur-3xl" /><div className="absolute -bottom-40 -left-20 h-96 w-96 rounded-full bg-accent-500/20 blur-3xl" /><div className="relative flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-white font-display text-xl font-extrabold text-primary-700">S</div><span className="font-display text-xl font-extrabold">SprintDesk</span></div><div className="relative m-auto max-w-lg"><div className="mb-7 inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1.5 text-xs font-semibold text-primary-100"><Sparkles size={14} />Built for focused teams</div><h1 className="font-display text-5xl font-extrabold leading-[1.08] tracking-tight">Make every sprint<br /><span className="text-primary-300">count.</span></h1><p className="mt-6 max-w-md text-lg leading-relaxed text-primary-100/75">A calm, clear workspace for teams who care about shipping meaningful work.</p><div className="mt-12 grid grid-cols-3 gap-4"><div><p className="font-display text-2xl font-bold">24%</p><p className="mt-1 text-xs text-primary-200/70">Faster delivery</p></div><div><p className="font-display text-2xl font-bold">4.9/5</p><p className="mt-1 text-xs text-primary-200/70">Team satisfaction</p></div><div><p className="font-display text-2xl font-bold">12k+</p><p className="mt-1 text-xs text-primary-200/70">Tasks shipped</p></div></div></div><p className="relative text-xs text-primary-200/60">© 2024 SprintDesk Inc.</p></div><div className="flex items-center justify-center bg-[#f7f8fc] p-6 dark:bg-[#111522] sm:p-10"><div className="w-full max-w-md"><div className="mb-10 lg:hidden"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary-600 font-display text-xl font-extrabold text-white">S</div><span className="font-display text-xl font-extrabold text-slate-900 dark:text-white">SprintDesk</span></div></div><div className="mb-8"><p className="mb-3 flex items-center gap-2 text-sm font-bold text-primary-600"><LockKeyhole size={16} />Welcome back</p><h2 className="font-display text-3xl font-extrabold tracking-tight text-slate-900 dark:text-white">Sign in to your workspace</h2><p className="mt-3 text-sm leading-relaxed text-slate-500">Keep your team moving forward, one task at a time.</p></div><form onSubmit={submit} className="space-y-5"><Input label="Username" value={username} onChange={(event) => setUsername(event.target.value)} placeholder="Enter your username" autoComplete="username" required /><div className="relative"><Input label="Password" type={showPassword ? 'text' : 'password'} value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Enter your password" autoComplete="current-password" required /><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-9 text-slate-400" aria-label={showPassword ? 'Hide password' : 'Show password'}>{showPassword ? <EyeOff size={17} /> : <Eye size={17} />}</button></div>{error && <p className="rounded-xl bg-error-50 px-3 py-2.5 text-sm font-medium text-error-700 dark:bg-error-950/30 dark:text-error-300">{error} Try the demo credentials: emilys / emilyspass.</p>}<Button type="submit" className="w-full py-3" disabled={isLoading}>{isLoading ? 'Signing you in...' : <>Sign in <ArrowRight size={17} /></>}</Button></form><p className="mt-7 text-center text-xs text-slate-400">Use <span className="font-bold text-slate-600 dark:text-slate-300">emilys</span> and <span className="font-bold text-slate-600 dark:text-slate-300">emilyspass</span> to explore the demo.</p></div></div></div>;
}
